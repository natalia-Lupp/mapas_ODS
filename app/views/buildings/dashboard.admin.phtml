<div class="mb-0">
    <?php include __DIR__ . '/../components/navbar.admin.phtml'; ?>
</div>

<div class="sidebar">
    <?php include __DIR__ . '/../components/sidenav.admin.phtml'; ?>
</div>

<div id="main-content">
    <div class="container-fluid pt-5 mt-4 p-0 px-5">
        <div class="row">
            <div class="col-12">
                <h2 class="mt-4 fw-semibold">Painel de Prédios</h2>
                <p class="text-muted">Gerencie as informações dos prédios cadastrados.</p>

                <!-- Cards de resumo -->
                <?php
                // Pega todos os prédios do paginator
                $buildings = $paginator->registers() ?? [];
                ?>
                <div class="row mb-4 g-3 d-flex justify-content-center">
                    <div class="col-sm-6 col-md-4 col-lg-3">
                    </div>
                </div>

                <!-- Tabela de prédios -->
                <div class="card shadow-sm border-0">
                    <div class="table-responsive">
                        <table id="tabelaPrediosDashboard" class="table align-middle text-center">
                            <thead class="table-primary">
                                <tr>
                                    <th>Prédio</th>
                                    <th>Andares</th>
                                    <th> </th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php if (!empty($buildings)): ?>
                                    <?php foreach ($buildings as $predio): ?>
                                        <tr>
                                            <td class="text-secondary"><?= htmlspecialchars($predio->name) ?></td>
                                            <td class="text-secondary"><?= $predio->n_floors ?></td>
                                            <td>
                                                <div class="d-flex justify-content-center gap-4">
                                                    <a href="/buildings/<?= $predio->id ?>/edit" class="btn btn-link text-primary p-0">
                                                        Editar
                                                    </a>
                                                    <form method="POST" action="/buildings/<?= $predio->id ?>" class="d-inline">
                                                        <input type="hidden" name="_method" value="DELETE">
                                                        <button class="btn btn-link text-danger p-0" type="submit">Excluir</button>
                                                    </form>
                                                </div>
                                            </td>
                                        </tr>
                                    <?php endforeach; ?>
                                <?php else: ?>
                                    <tr>
                                        <td colspan="3" class="text-muted">Nenhum prédio cadastrado.</td>
                                    </tr>
                                <?php endif; ?>
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- Botões abaixo da tabela -->
                <div class="d-flex justify-content-between align-items-center mt-5 mb-5">
                    <form method="POST" action="/buildings/deleteAll">
                        <input type="hidden" name="_method" value="DELETE">
                        <button class="btn btn-outline-danger fw-semibold px-4 rounded-pill" type="submit">
                            Excluir Todos
                        </button>
                    </form>

                    <button class="btn btn-primary fw-semibold px-4 rounded-pill" id="btnExportarPDF">
                        Exportar PDF
                    </button>
                </div>

            </div>
        </div>
    </div>
</div>
