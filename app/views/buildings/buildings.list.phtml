<div class="mb-0">
    <?php include __DIR__ . '/../components/navbar.admin.phtml'; ?>
</div>

<div class="sidebar">
    <?php include __DIR__ . '/../components/sidenav.admin.phtml'; ?>
</div>

<div id="main-content">
    <div class="container-fluid pt-5 mt-4 p-0 px-5">
        <div class="col-12">
            <h2 class="mt-4 fw-semibold">Lista de Prédios</h2>
            <p class="text-muted">Todos os prédios cadastrados</p>

            <?php
            // Consulta todos os prédios do banco
            $stmt = $pdo->query("SELECT name, n_floors FROM buildings ORDER BY id DESC");
            $predios = $stmt->fetchAll(PDO::FETCH_ASSOC);
            ?>

            <div id="tabelaPrediosList" class="mt-4">
                <?php if (empty($predios)): ?>
                    <p class="text-muted">Nenhum prédio cadastrado.</p>
                <?php else: ?>
                    <table class="table table-striped">
                        <thead>
                            <tr>
                                <th>Nome do Prédio</th>
                                <th>Número de Andares</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($predios as $p): ?>
                                <tr>
                                    <td><?= htmlspecialchars($p['name']) ?></td>
                                    <td><?= htmlspecialchars($p['n_floors']) ?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                <?php endif; ?>
            </div>

            <div class="mt-4">
                <a href="<?= route('buildings.new') ?>" class="btn btn-primary rounded-pill px-4">
                    Cadastrar Novo Prédio
                </a>
            </div>
        </div>
    </div>
</div>