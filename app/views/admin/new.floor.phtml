<div class="mb-0">
    <?php include __DIR__ . '/../components/navbar.admin.phtml'; ?>
</div>

<div class="sidebar">
    <?php include __DIR__ . '/../components/sidenav.admin.phtml'; ?>
</div>

<div id="main-content">
    <div class="container-fluid pt-5 mt-4 p-0 px-5">
        <div class="col-12">
            <h2 class="mt-4 fw-semibold">Adicionar Andar e Banheiros ao Prédio</h2>
            <p class="text-muted">Prédio ID: (não definido ainda)</p>

            <div class="form-background">
                <form id="formAdicionarAndar">
                    <div class="mb-3">
                        <label for="numeroAndar" class="form-label fw-semibold">Escolha o Andar</label>
                        <select name="numeroAndar" id="numeroAndar" class="form-select" required>
                            <?php for ($i = 1; $i <= 10; $i++): ?>
                                <option value="<?= $i ?>">Andar <?= $i ?></option>
                            <?php endfor; ?>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label for="banheiros" class="form-label fw-semibold">Número de Banheiros</label>
                        <select name="banheiros" id="banheiros" class="form-select" required>
                            <?php for ($b = 1; $b <= 7; $b++): ?>
                                <option value="<?= $b ?>"><?= $b ?> banheiro(s)</option>
                            <?php endfor; ?>
                        </select>
                    </div>

                    <div class="d-flex justify-content-between mt-4">
                        <button type="button" class="btn btn-primary rounded-pill px-4 d-flex align-items-center gap-2">
                            Adicionar Andar
                        </button>
                        <p id="mensagemErro" class="text-danger fw-semibold mt-2" style="display: none;">
                            Nenhum prédio encontrado. Cadastre um prédio primeiro.
                        </p>
                    </div>

                    <div class="d-flex justify-content-between mt-4">
                        <a href="/admin/new.building" class="btn btn-voltar rounded-pill px-4">Voltar</a>
                        <button type="button" id="btnCadastrarItens" class="btn btn-primary rounded-pill px-4 d-flex align-items-center gap-2">
                            <i class="bi bi-building-add"></i>
                            Cadastrar Itens
                        </button>

                    </div>
                </form>
            </div>
            <div id="listaAndares" class="mt-3"></div>
        </div>
    </div>
</div>