<div class="mb-0">
    <?php include __DIR__ . '/../components/navbar.admin.phtml'; ?>
</div>

<div class="sidebar">
    <?php include __DIR__ . '/../components/sidenav.admin.phtml'; ?>
</div>

<div id="main-content">
    <div class="container-fluid pt-5 mt-4 p-0 px-5">
        <div class="col-12">
            <h2 class="mt-4 fw-semibold">Adicionar Itens aos Banheiros do Prédio</h2>
            <p class="text-muted">Prédio ID: (não definido ainda)</p>

            <div class="form-background">
                <form id="formCadastroItens">
                    <div class="mb-3">
                        <label for="qtdTorneiras">Número de Torneiras</label>
                        <select id="qtdTorneiras" name="qtdTorneiras" class="form-select">
                            <?php for ($i = 1; $i <= 30; $i++): ?>
                                <option value="<?= $i ?>"><?= $i ?> torneira(s)</option>
                            <?php endfor; ?>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label for="qtdVasos">Número de Vasos</label>
                        <select id="qtdVasos" name="qtdVasos" class="form-select">
                            <?php for ($i = 1; $i <= 30; $i++): ?>
                                <option value="<?= $i ?>"><?= $i ?> vaso(s)</option>
                            <?php endfor; ?>
                        </select>
                    </div>
                    <div class="d-flex justify-content-between mt-4">
                        <button type="button" id="btnAddItens" class="btn btn-primary rounded-pill px-4 d-flex align-items-center gap-2">
                            Adicionar Itens
                        </button>
                    </div>

                    <div class="d-flex justify-content-between mt-4">
                        <a href="/admin/new.building" class="btn btn-voltar rounded-pill px-4">Voltar</a>
                        <button type="button" id="btnSalvarPredio" class="btn btn-primary rounded-pill px-4 d-flex align-items-center gap-2">
                            <i class="bi bi-building-add"></i>
                            Salvar
                    </div>
                    <p id="mensagemErroItens" class="text-danger fw-semibold mt-3 d-none"></p>
                    <p id="mensagemSucesso" class="text-success fw-semibold mt-3 d-none">Itens cadastrados com sucesso!</p>
                </form>
            </div>

            <div id="containerTabelaItens" class="mt-4"></div>

        </div>
    </div>
</div>