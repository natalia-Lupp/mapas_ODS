<h2 class="mt-4 fw-semibold">Cadastrar Nova Estrutura</h2>
<p class="text-muted">Informe os dados do novo prédio</p>

<div class="form-background">
    <form id="formCadastroPredio" method="POST" action="<?= route('admin.buildings.create') ?>" novalidate>

        <div class="mb-3">
            <label for="nomePredio" class="form-label fw-semibold">Nome do Prédio</label>
            <input type="text" class="form-control" id="nomePredio" placeholder="Digite o nome do prédio"> <!-- id tem q ser id_nome(atibuto) e nome tem que ser nome="buildngs[nome do que faz o input]-->
        </div>

        <div class="mb-3">
            <label for="numeroAndares" class="form-label fw-semibold">Número de Andares</label>
            <select class="form-select" id="numeroAndares">
                <?php for ($i = 1; $i <= 10; $i++): ?>
                    <option value="<?= $i ?>"><?= $i ?> andar<?= $i > 1 ? 'es' : '' ?></option>
                <?php endfor; ?>
            </select>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <button type="button" id="btnAddPredios" class="btn btn-primary rounded-pill px-4 d-flex align-items-center gap-2">
                Adicionar Prédio
            </button>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <a href="<?= route('admin.buildings.index') ?>" class="btn btn-voltar rounded-pill px-4">Voltar</a>
            <button type="submit" class="btn btn-primary rounded-pill px-4 d-flex align-items-center gap-2">
                <i class="bi bi-building-add"></i>
                Salvar Prédio
            </button>
        </div>

    </form>
</div>

<div id="containerTabelaPredios" class="mt-4">
    <p class="text-muted">Nenhum prédio adicionado.</p>
</div>