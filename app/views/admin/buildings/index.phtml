<h2 class="mt-5 fw-semibold">Painel de Prédios</h2>
<p class="text-muted">Gerencie as informações dos prédios cadastrados.</p>

<!-- Cards de resumo -->
<?php
// Pega todos os prédios do paginator

use Behat\Gherkin\Filter\RoleFilter;

$buildings = $paginator->registers() ?? [];
?>
<div class="row mb-4 g-3 d-flex justify-content-center">
    <div class="col-sm-6 col-md-4 col-lg-3">
    </div>
</div>

<!-- Tabela de prédios -->
<div class="card shadow-sm border-0 mb-5">
    <div class="table-responsive">
        <table id="tableBuildingsDashboard" class="table align-middle text-center">
            <thead class="table-primary">
                <tr>
                    <th>Prédio</th>
                    <th>Andares</th>
                    <th> </th>
                </tr>
            </thead>
            <tbody>
                <?php if (!empty($buildings)): ?>
                    <?php foreach ($buildings as $building):
                        $id = $building->id;
                    ?>
                        <tr>
                            <td class="text-secondary"><?= htmlspecialchars($building->name) ?></td>
                            <td class="text-secondary"><?= $building->n_floors ?></td>
                            <td>
                                <div class="d-flex justify-content-center gap-4">
                                    <a href="<?= route('admin.buildings.show', ['id' => $building->id]) ?>" class="btn btn-link text-primary p-0 link-details-<?= $id ?>">
                                        Detalhes
                                    </a>
                                    <a href="<?= route('admin.buildings.bathrooms.index', ['building_id' => $building->id]) ?>" class="btn btn-link text-primary p-0 link-bathrooms-<?= $id ?>">
                                        Banheiros
                                    </a>

                                    <a href="<?= route('admin.buildings.edit', ['id' => $building->id]) ?>" class="btn btn-link text-primary p-0 link-edit-<?= $id ?>">
                                        Editar
                                    </a>
                                    <form method="POST" action="<?= route('admin.buildings.destroy', ['id' => $building->id]) ?>" class="d-inline">
                                        <input type="hidden" name="_method" value="DELETE">
                                        <button class="btn btn-link text-danger p-0 link-delete-<?= $id ?>" type="submit">Excluir</button>
                                    </form>

                                </div>
                            </td>
                        </tr>
                    <?php endforeach; ?>
                <?php else: ?>
                    <tr>
                        <td colspan="3" class="text-muted">Nenhum prédio cadastrado.</td>
                    </tr>
                <?php endif; ?>
            </tbody>
        </table>
        <?php $paginator->renderPagesNavigation(); ?>
    </div>
</div>
