<h2 class="mt-5 fw-semibold"><?= htmlspecialchars($title) ?></h2>
<p class="text-muted">Atualize os dados do banheiro</p>
<div class="form-background">
    <form id="formEditarBanheiro" method="POST"
        action="<?= route('admin.buildings.bathrooms.update', ['id' => $bathroom->id]) ?>"> <input type="hidden" name="_method" value="PUT"> <!-- Prédio fixo -->
        <div class="mb-3">
            <label class="form-label fw-semibold">Nome do prédio</label>
            <p class="form-control-plaintext"><?= htmlspecialchars($building->name) ?></p>
            <input type="hidden" name="bathroom[building_id]" value="<?= $building->id ?>" class="field-building_id">
        </div> <!-- Andar -->
        <div class="mb-3">
            <label class="form-label fw-semibold">Andar</label>
            <select class="form-select field-floor" id="id_floor" name="bathroom[floor]" required>
                <?php for ($i = 0; $i < $building->n_floors; $i++): ?>
                    <option
                        value="<?= $i ?>"
                        <?= $i == $bathroom->floor ? 'selected' : '' ?>>
                        <?= $i + 1 ?>º andar
                    </option>
                <?php endfor; ?>
            </select>
        </div>


        <?php foreach ($types as $type): ?>
            <div class="mb-3">
                <label class="form-label fw-semibold"><?= $type->name ?></label>
                <select class="form-select field-item-<?= $type->id ?>" name="items[<?= $type->id ?>]" required>
                    <?php for ($i = 0; $i <= 50; $i++): ?>
                        <option value="<?= $i ?>"
                            <?= $bathroom->lengthOfItem($type) == $i ? 'selected' : '' ?>>
                            <?= $i ?>
                        </option>
                    <?php endfor; ?>
                </select>
            </div>
        <?php endforeach; ?>

        <div class="d-flex justify-content-between mt-4">
            <a href="<?= route('admin.buildings.bathrooms.index', ['building_id' => $building->id]) ?>"
                class="btn btn-voltar rounded-pill px-4">
                Voltar
            </a>
            <button type="submit"
                class="btn btn-primary rounded-pill px-4 d-flex align-items-center gap-2 link-submit">
                <i class="bi bi-save"></i>
                Atualizar Banheiro
            </button>
        </div>
    </form>
</div>
<script src="/assets/js/admin/bathroom/new.js"></script>
