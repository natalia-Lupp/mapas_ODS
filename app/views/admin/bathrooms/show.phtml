<?php

use App\Services\Image;

/** @var \App\Models\Bathroom $bathroom */
?>

<h2 class="mt-5 fw-semibold"><?= htmlspecialchars($titleNome) ?></h2>
<p class="text-muted">Visualize as informações do Banheiro cadastrado.</p>

<div class="row mt-4 mb-5">
    <div class="col-md-12">
        <div class="card shadow-sm border border-primary border-2 p-5">
            <h4 class="fw-semibold mb-3">Informações gerais</h4>

            <!-- 🔹 Texto fake de demonstração -->
            <div class="mb-3">
                <span class="fw-semibold text-secondary d-block mb-1">Resumo dos itens</span>
                <p class="mb-0">
                    Este banheiro possui <strong>8 itens</strong> no total, sendo:
                    <strong>3 vasos sanitários</strong> e <strong>5 torneiras</strong> (texto fake para testar opções de layout).
                </p>
            </div>

            <h4 class="mt-5 mb-4    fw-semibold"> Galeria de Imagens </h4>

            <!-- 🔹 Exibir todas as imagens associadas -->
            <div class="mb-2 d-flex flex-wrap gap-3">
                <?php if (!empty($bathroom->images)): ?>
                    <?php foreach ($bathroom->images as $image): ?>
                        <div class="bathroom_image_wrapper">
                            <img
                                class="bathroom_image border rounded shadow-sm"
                                src="<?= htmlspecialchars($image->imageService()->path()); ?>"
                                alt="Imagem do banheiro"
                                style="width: 150px; height: 150px; object-fit: cover;">
                        </div>
                    <?php endforeach; ?>
                <?php else: ?>
                    <p class="text-muted">Nenhuma imagem adicionada ainda.</p>
                <?php endif; ?>
            </div>

            <div class="d-flex justify-content-between mt-4">
                <a href="<?= route('admin.buildings.bathrooms.index', [
                                'building_id' => $bathroom->building_id
                            ]) ?>"
                    class="btn btn-voltar rounded-pill px-4 link-comeback">
                    Voltar
                </a>

                <!-- 🔹 Botão que mostra o formulário -->
                <button
                    type="button"
                    class="btn btn-primary rounded-pill px-4"
                    id="showFormButton">
                    Adicionar imagem
                </button>
            </div>
        </div>
    </div>
</div>

<!-- 🔹 Formulário oculto inicialmente -->
<div id="formContainer" class="form-background mt-4 mb-5" style="display: none;">
    <form method="POST" action="<?= route('admin.buildings.bathrooms.images.create', [
                                    'building_id' => $bathroom->building_id,
                                    'id' => $bathroom->id
                                ]) ?>" enctype="multipart/form-data">

        <div class="mb-3">
            <label for="bathroom_image" class="form-label">Adicionar nova imagem</label>
            <input
                class="form-control field-image"
                type="file"
                id="bathroom_image"
                name="bathroom_image"
                accept="image/*"
                multiple>
        </div>

        <div class="d-flex justify-content-between mt-4">
            <button type="submit" class="btn btn-primary rounded-pill px-4 d-flex align-items-center gap-2 link-submit">
                <i class="bi bi-upload"></i>
                Enviar Imagem
            </button>
        </div>
    </form>
</div>

<!-- 🔹 Importa o JavaScript externo -->
<script src="/assets/js/admin/bathroom/show.js"></script>