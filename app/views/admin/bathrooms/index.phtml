<h2 class="mt-5 fw-semibold"><?= $title ?></h2>
<p class="text-muted">Quer adicionar um banheiro?</p>

<?php
$bathrooms = $paginator->registers() ?? [];
$buildingIdForRoute = isset($building) ? $building->id : 0;
?>

<div>
    <?php if ($buildingIdForRoute > 0): ?>
        <a class="btn btn-primary fw-semibold px-4 rounded-pill mb-3"
            href="<?= route('admin.buildings.bathrooms.new', ['building_id' => $buildingIdForRoute]) ?>">
            Novo banheiro
        </a>
    <?php endif; ?>

    <table id="tableBuildingsDashboard" class="table align-middle text-center">
        <thead class="table-primary">
            <tr>
                <th>Prédio</th>
                <th>Andar</th>
                <th>Total de Itens (ideia)</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            <?php if (!empty($bathrooms)): ?>
                <?php foreach ($bathrooms as $bathroom):
                    $building = $bathroom->building;
                ?>
                    <tr>
                        <td class="text-secondary"><?= htmlspecialchars($building->name ?? '—') ?></td>
                        <td class="text-secondary"><?= $bathroom->floor ?></td>
                        <td> aqui vai os itens totais</td>
                        <td>
                            <div class="d-flex justify-content-center gap-2">

                                <a href="<?= route('admin.buildings.bathrooms.show', [
                                                'building_id' => $bathroom->building_id,
                                                'id' => $bathroom->id
                                            ]) ?>"
                                    class="btn btn-link text-primary p-0 link-details-<?= $bathroom->id ?>">
                                    Detalhes
                                </a>

                                <a href="<?= route('admin.buildings.bathrooms.edit', [
                                                'building_id' => $bathroom->building_id,
                                                'id' => $bathroom->id
                                            ]) ?>"
                                    class="btn btn-link text-primary p-0 link-edit-<?= $bathroom->id ?>">
                                    Editar
                                </a>

                                <a href="<?= route('admin.buildings.bathrooms.items.index', [
                                                'building_id' => $bathroom->building_id,
                                                'bathroom_id' => $bathroom->id
                                            ]) ?>"
                                    class="btn btn-link text-primary p-0 link-edit-<?= $bathroom->id ?>">
                                    Itens
                                </a>


                                <form method="POST"
                                    action="<?= route('admin.buildings.bathrooms.destroy', [
                                                'building_id' => $bathroom->building_id,
                                                'id' => $bathroom->id
                                            ]) ?>"
                                    class="delete-form delete-form-<?= $bathroom->id ?>"
                                    style="display:inline-block">
                                    <input type="hidden" name="_method" value="DELETE">
                                    <button type="button"
                                        class="btn btn-link text-danger p-0 btn-delete link-delete-<?= $bathroom->id ?>"
                                        data-id="<?= $bathroom->id ?>">
                                        Excluir
                                    </button>
                                </form>

                            </div>
                        </td>

                    </tr>
                <?php endforeach; ?>
            <?php else: ?>
                <tr>
                    <td colspan="3" class="text-muted">Nenhum banheiro cadastrado.</td>
                </tr>
            <?php endif; ?>
        </tbody>
    </table>

    <?php $paginator->renderPagesNavigation(); ?>

    <div class="d-flex justify-content-between mt-4 mb-5">
        <a href="<?= route('admin.buildings.index') ?>" class="btn btn-voltar rounded-pill px-4 link-comeback">Voltar</a>
    </div>
</div>

<!--modal com confirmação de exclusão -->
<div class="modal fade" id="deleteConfirmModal" tabindex="-1" aria-labelledby="deleteConfirmLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="deleteConfirmLabel">Confirmar exclusão</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fechar"></button>
            </div>
            <div class="modal-body">
                Tem certeza que deseja excluir este banheiro?
                <br>Pode outros itens e imagens vinculados a ele.
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary rounded-pill px-4" data-bs-dismiss="modal">Cancelar</button>
                <button type="button" class="btn btn-danger rounded-pill px-4" id="confirmDeleteBtn">Excluir</button>
            </div>
        </div>
    </div>
</div>

<script src="/assets/js/admin/bathroom/index.js"></script>